#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *
 * @int: an absolute value
 *
 * Return: value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
